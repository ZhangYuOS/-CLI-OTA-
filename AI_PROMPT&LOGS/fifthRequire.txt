Act as an expert Python Software Engineer. The advanced OTA features are working perfectly. Now we need to implement Step 4 to bring this project to an industrial standard: Resumable Downloads and Device Telemetry.

IMMEDIATE ACTION (Step 4 - Industrial Robustness & Telemetry):

1. Resumable Downloads (断点续传)
   - Server (`server/main.py`): Update the `/firmware` endpoint to support HTTP Range requests. Read the `Range` header from the request. If present, return a `206 Partial Content` response streaming only the requested byte range. Set the `Accept-Ranges: bytes` and `Content-Range` headers properly.
   - Client (`client/cli.py`): In the `update` command, before downloading, check if a partial `firmware.bin` exists in the target standby slot. If it does, get its size in bytes, and add the `Range: bytes={size}-` header to the requests.get() call. Open the file in append mode (`"ab"`). Ensure the Rich progress bar correctly reflects the starting offset so it doesn't start at 0%.

2. Device Telemetry / Status Reporting (状态上报)
   - Client (`client/device_status.json`): Add a new key `"device_id": "DEV-RK3588-001"` to the initial state.
   - Client (`client/cli.py`): Create a helper function `report_to_server(status_msg)`. This function should send a POST request to `<server_url>/report` containing a JSON payload: `{"device_id": ..., "active_slot": ..., "version": ..., "status": status_msg}`.
   - Trigger points: Call this function at the end of a successful `update` (e.g., status="Update Successful"), after an auto or manual `rollback` (e.g., status="Rollback Triggered"), and inside the `KeyboardInterrupt` / Abort block (e.g., status="Download Aborted").
   - Server (`server/main.py`): Add a `POST /report` endpoint that accepts this JSON payload. For now, just pretty-print the received telemetry data to the server's console using Python's standard `logging` or print statements with a clear prefix like `[TELEMETRY]`.

3. Documentation
   - Document this Step 4 in `docs/AI_Prompt_Log.md` using your [prompt-log-tracker] skill.

Stop after completing Step 4 and wait for my review.